global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: dataforge

scrape_configs:
  - job_name: dataforge-backend
    static_configs:
      - targets: ["backend:8000"]
    metrics_path: /metrics
